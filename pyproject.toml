[project]
name = "termo-pipeline"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "lxml>=6.1.1",
    "rapidfuzz>=3.14.5",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
